extends layouts/main
include mixins/videoBlock

block content
    .videos_list
        each item in videos /* videoController.js에서 await Video.find({});로 만들어진 객체 */
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile: item.fileUrl
            })